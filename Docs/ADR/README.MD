# 設計判断の記録（ADR）

> Architecture Decision Record — 「なぜそのとき、そう決めたか」を一件ずつ残す記録です。
> 結論だけでなく、検討して捨てた案とその理由まで書いています。目的は、後からその決定を
> *背景を知らないまま元に戻してしまう*ことを防ぐこと。決定を凍結するためではなく、根拠を
> 残すためのものです（根拠が変われば、堂々と直せるように）。

このフォルダは、戦闘中のAIをどう設計したかの記録です。前半（0001〜0004）は仲間AI（味方
キャラの立ち位置・移動・行動）、後半（0005〜0006）は敵グループのAIを扱います。コードを
読まなくても設計の意図と思考の流れが追えるように書いています。

---

## 読む順番

初めての方は **概観 → 番号順** で読むと、全体像から具体へと辿れます。

1. まず **[アーキテクチャ概観](0000-architecture_overview.MD)** で全体の層分担
   （隊形＝立ち位置の提供、ステートマシン＝行動の状態、調停役＝最終的な移動）をつかむ。
2. **0001〜0004は仲間（戦闘中の味方）AI**、**0005〜0006は敵グループのAI**です。二つの流れは
   独立しているので、興味のある方から読んでも構いません。

---

## 記録の一覧

| # | タイトル | 状態 | 一言で |
|---|---------|------|-------|
| [概観](0000-architecture_overview.MD) | 戦闘中の移動・立ち位置・行動の層分担 | 生きた文書 | 戦闘中の最終的な位置は、立ち位置の座標一つでは決まらない。隊形は基準点（ホーム）だけを提供し、行動ステートマシンが状態を、単一の調停役が複数の移動の意図から実行する一つを決める。複数の決定が共有する上位の枠組み。（※ 実装済みは立ち位置の提供まで。行動の層・調停役は設計の方針。） |
| [0001](0001-traversal_decision_authority.MD) | ジャンプを「するか」決めるのは誰か | 採用 | ジャンプを*するべきか*（状況判断）は「今その瞬間の単一の主体」が決め、共有しない。ジャンプが*物理的に可能か*とその実行は、誰が呼んでも同じ共通の道具が担う。判断と物理を分けた。 |
| [0002](0002-slot_generation_abstraction.MD) | 立ち位置の決め方の抽象化 | 採用 | 立ち位置を計算する契約を「一人につき一つの場所」に統一。全員の巡回は呼び出し側が回し、「全員まとめて」型（扇形）は人数と番号から、「一人ずつ」型（遠距離）は自分の射程と敵の配置から計算する。職が方式を*選ぶ*（職＝方式ではない）。 |
| [0003](0003-battleformation_decision_execution_split.MD) | 「決める」と「動く」を分ける | 採用 | 影響マップの立ち位置は、毎フレーム押し続ける操縦士ではなく、きっかけのときだけ決める相談役であるべき。初回・射程から外れた・脅威が近い、のときだけ再配置し、その間は立ち位置を固定する。プレイヤーはきっかけではない。 |
| [0004](0004-rangedsafe_omnidirectional_influence_map.MD) | 遠距離キャラの立ち位置 — 方向を決めず評価に委ねる | 採用 | 遠距離の立ち位置を、「先に方向を決めて狭い扇形に候補を置く」やり方から、方向を決めずターゲット周囲360度に候補をばらまき、判断はすべて評価（脅威・味方との距離・重なり）に委ねるやり方へ。方向の軸がないので、プレイヤーの動きで候補が回転するブレが消えた。このシンプルさは作りかけではなく、実測の末に辿り着いた結果。 |
| [0005](0005-enemy_group_spatial_partition_hash_grid.MD) | 敵グループの空間分割 — 疎な格子の採用と限界 | 採用 | 敵グループの活性・生成の判定を毎フレームの全走査なしに行うため、疎な均一格子（Sparse Spatial Hash Grid）を採用。木構造（BSP/KD/octree）はデータの性質（静的・均等・小規模）に合わず見送り。このシンプルさは妥協ではなく適合の結果。「空間分割に完全な理論はない」（密集・再登録）という学びも記録。 |
| [0006](0006-enemy_group_combat_architecture.MD) | 敵グループ戦闘アーキテクチャ — 戦略をモンスターの種別から切り離す | 採用 | グループの隊形（包囲/Cなど）をモンスターの種別に固定せず、グループへ割り当てる。多様性（個別の気質）はモンスターに、戦略（隊形）はグループに置いてこそ両立する。グループの境界はスポーンプールが決め（同じA,Bでもプール次第で2グループ/1グループ）、合成はプール依存、役割と種別の分離、「破綻する動作だけ扱い、まずい配置は許す」。契約の境界（意図だけ伝える）で、GASを後から差し込む余地を確保。 |

> 📌 **番号について：** 「戦闘行動の層分担」は当初0004として書き始めたが、単一の決定ではなく
> 全体の枠組みだと分かったため**概観へ格上げ**した（番号の体系の外）。空いた0004は「遠距離の
> 360度の評価」が引き継いでいる。初期段階なので番号は欠番を作らず連番で整理した（外部からの
> 参照が増えた後は、こうした番号の振り直しはしない）。

---

## まだ開いている問い（今後の検討）

- **場面の切り替えを誰がトリガーするか** — 戦闘中、判断の主体が隊形からステートマシンへ移る、
  その切り替えを誰がどう起こすか。ステートマシンの設計の本体で扱う。（ADR-0001、概観で「どの
  システムがどの意図を出す資格を持つか」へ吸収予定。）
- **「一人ずつ」型と移動交差の最小化の関係** — 遠距離のように一人ずつ位置を出す方式で、グループ
  内の移動交差をどこまで整理するか。一人用の契約が番号どおりの対応を構造的に和らげるが、射程差が
  大きいときの整理は実測が要る。（ADR-0002）
- **複数の職が一つの配置を共有する場合** — 「近接と盾役が一つの扇形」のような要求が出たら、役割と
  グループの1対1の仮定とぶつかる。グループ分けのキーの再設計が候補。（ADR-0002）
- **再配置の猶予を、出発時ではなく到着時から数える** — 再配置の猶予が到着ではなく出発から数え
  始めるため、移動が長いと猶予が薄れる。敵が動くようになり「到着」の状態ができたら、到着のイベントを
  基準に精密化する。静止したダミーの的では影響がないため現在は見送り。（ADR-0003、概観）
- **立ち位置の周りで小さく流す（3.10）の実装** — 錨（立ち位置）は離散的に決め、その狭い周りでだけ
  小さな力場で流す「水のように流れる」挙動。行動の層が入った後、最も低い優先度の意図の一部として
  実装する。（概観 3.10、ADR-0004の捨てた案Dから分離）
- **敵グループの役割ベースのスロット割り当て** — C戦略の「近接は前列／遠隔は後列」は、既存の仲間の
  割り当て（同質に扱う）に気質ベースのコストを足す拡張が要る。敵グループにそのまま使うかは未確定。
  （ADR-0006）
- **戦略リゾルバの解釈順** — スポーンプールの合成ルールと、気質に基づく基本編成を、どの順で解釈
  するか。スポーン時に一度動く構造として方向だけ定めた。次のADRの候補。（ADR-0006）
- **敵グループのLOD・活性段階の詳細** — 疎な格子の上で、交戦・徘徊・非活性の段階をどう分け、どう
  遷移させるか（移動ゲート、ヒステリシスの帯）は0005で方向のみ。実装時に具体化する。（ADR-0005）

---

## 書き方の原則（すべての記録に共通）

- **圧縮して書くと数週間後に自分が読めない。** 決定は「なぜ」を文章で書く。
- **検討して捨てた案とその理由を必ず残す。** 後でその案を再び思いついたとき「それは試して、
  こういう理由で合わなかった」とすぐ読めるように。同じ失敗の繰り返しは防ぎ、再挑戦の道は開けておく。
  - **残すなら「読める形で」残す。** 表や結論だけに圧縮すると、数日後には検証できず再び調べ直す
    ことになる。案ごとに ① 一文で「形」（対象が多いマスだけ四分割、など）→ ② 次に限界 → ③ 抽象語
    ではなくゲーム内の場面で（剣が敵に当たったか、など）→ ④ 抽象的な条件は具体的な数字で（スライム
    半径300／ボス半径10000、など）。目的は*短く*ではなく*速く理解*。
- **凍結ではなく根拠の保存。** 「この構造は絶対に直さない」とは書かない。根拠（現象・限界・制約）を
  残し、それが変われば堂々と直せるようにする。防ぎたいのは*背景を知らないまま元に戻すこと*だけ。
- **検証の範囲を超える断定をしない。** 「解決した」「完全に消えた」より、「改善した」「抑えやすく
  なった」のように範囲を限って書く。成果を語るときは、何が良くなったかと並べて、何はまだ解決して
  いないかも書く。